//Here you should fill with the correct routes

import http from "../http-common";

const getAll = () => {
};

const get = id => {
};

const create = data => {
};

const update = (id, data) => {
};

const remove = id => {
};

const removeAll = () => {
};

const findByTitle = title => {
};

export default {
  getAll,
  get,
  create,
  update,
  remove,
  removeAll,
  findByTitle
};
